// Imports all components for the demo purpose only
// In production, importing only the needed components is recommended
import '@provetcloud/web-components'

export default defineNuxtPlugin(() => {});